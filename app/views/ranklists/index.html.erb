<% content_for :title, "Kerala PSC Rank List" %>
<%if current_user.role == 'normal' %>
	<%= render partial: 'user_rank' %>
	</div>
	<%= will_paginate @user_exams %>
<% else %>
	<%= link_to image_tag("fb_login.png", style: "display: block;margin-left:auto;margin-right:auto;") %>
<% end %>
